# PRD — FABOT 투자 신호·잔고 안내 챗봇

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 서비스명 | FABOT 투자 신호·잔고 안내 챗봇 |
| 작성일 | 2026-08-27 |
| 관련 문서 | [design-packet.md](design-packet.md), [tool-definition.md](my-agent/tool-definition.md), [README.md](README.md) |

## 2. 배경 및 문제 정의

FABOT은 F&G(공포·탐욕) 지수 기반 규칙 투자 전략을 운영하는 개인용 자동화 프로젝트다.
현재 신호 계산과 매매일지 기록은 자동화되어 있지만(24시간 F&G 계산 파이프라인,
매매 기록 DB), **사용자가 이 정보를 확인하려면 대시보드를 직접 열어봐야 한다.**
"오늘 매수해도 되나?", "이 종목 쿨다운 풀렸나?" 같은 단순 확인조차 여러 화면을
찾아다녀야 하는 불편이 있다.

## 3. 목표

- 자연어 질문 한 번으로 오늘의 F&G 신호, 특정 종목의 쿨다운 상태, 계좌 잔고/수익 추이,
  전략 규칙 원문을 즉시 확인할 수 있게 한다.
- **실제 주문 실행은 절대 하지 않는다** — 조회 전용으로 범위를 제한해, "잘못된 자동
  매매 실행"이라는 실패 유형 자체가 발생하지 않도록 설계한다.

### 성공 지표 (이번 과제 범위에서 검증한 기준)

| 지표 | 목표 | 검증 방법 |
|---|---|---|
| 정상 질문에 올바른 도구를 고르는가 | 테스트 문항 9개 중 정상/경계/공격 경로 모두 기대대로 동작 | design-packet.md ⑥ 테스트 문항 |
| 근거 없는 답을 사용자에게 보여주지 않는가 | 도구 미호출 시 모델의 원문 응답이 최종 사용자 화면에 노출되지 않음 | trace-05-hallucination.txt로 실측 확인 |
| 의미 기반 규칙 검색이 되는가 | "커버드콜은 언제 사?" 같은 질문에 관련 규칙이 top-2 안에 포함 | trace-04-rag.txt로 실측 확인 |

## 4. 대상 사용자

FABOT 전략을 따르는 개인 투자자 본인, 그리고 배당소득 분산을 위해 계좌를 나눠 쓰는
가족 구성원(지영/인선/준우). 이번 과제 범위에서는 **1인이 본인 계좌만 조회**하는
경우만 다루고, 가족 구성원별 인증·권한 분리는 범위 밖으로 명시한다(README.md 참고).

## 5. 범위

### 포함 (In Scope)

- 오늘 F&G 지수·신호 조회 (`get_fg_signal`)
- 종목별 매수 쿨다운 상태 조회 (`check_cooldown`)
- 계좌 잔고·평가손익 조회 (`get_balance`)
- 최근 N일 수익률 추이 조회 (`get_balance_history`)
- 전략 규칙 의미 기반 검색 (`search_strategy_rule`, RAG/임베딩)

### 제외 (Out of Scope) — 다음 단계로 이관

- 실제 매수·매도 주문 실행 (M11 해커톤에서 별도 승인 플로우와 함께 설계 예정)
- 가족 구성원별 계좌 인증·권한 분리
- 여러 말투 변형에 대한 회귀 테스트 자동화 (trace-05에서 발견한 "말투 차이로 도구
  호출 여부가 갈리는" 문제의 근본 해결)

## 6. 핵심 기능 요약

도구 계약 5개의 전체 스펙(인자, 반환 형식, 실패 시 행동)은 design-packet.md ②번
항목에 상세 정의되어 있다. 이 PRD에서는 "왜 이 5개인가"만 요약한다.

| 도구 | 왜 필요한가 |
|---|---|
| get_fg_signal | 오늘 매수/매도 신호를 즉시 확인 — 가장 빈번한 질문 |
| check_cooldown | 신호가 맞아도 쿨다운 중이면 매수 불가 — 신호만으로는 부족 |
| get_balance | 계좌 현황 확인은 신호 확인만큼 자주 필요 |
| get_balance_history | 단일 시점이 아니라 추이를 봐야 하는 질문 대응 |
| search_strategy_rule | 정형 조회로 안 되는 "규칙 자체가 뭐였지?" 질문 대응 (RAG) |

## 7. 비기능 요구사항

- **안전성 최우선**: 쓰기 도구(주문 실행) 자체를 포함하지 않아, 권한 설계 실수가
  자산 손실로 이어질 가능성을 원천 차단한다.
- **개인정보**: 계좌번호, API 키, 인증 토큰은 도구 반환값과 최종 답변 어디에도
  포함하지 않는다(design-packet.md ⑤).
- **근거 없는 확정 금지**: 도구를 호출하지 않고 모델이 자체 지식으로 답하려 한
  경우, 그 응답을 사용자에게 노출하지 않는다 — 실측 중 실제로 발생한 환각 사례
  (trace-05-hallucination.txt)를 막기 위해 하네스 레벨에서 강제한다.

## 8. 리스크와 완화

| 리스크 | 실측 근거 | 완화 방법 |
|---|---|---|
| 영어 전용 임베딩 모델의 한국어 검색 순위 오류 | trace-04-rag.txt (정답 규칙이 2등으로 밀림) | top-1이 아닌 top-2 이상 반환 |
| 소형 로컬 모델이 도구 호출 없이 사실과 다른 답을 지어냄 | trace-05-hallucination.txt (TQQQ 관련 완전 허구 답변) | 도구 미호출 응답은 사용자에게 비노출, 개발자 로그로만 보존 |
| 같은 의도라도 말투 차이로 도구 호출 여부가 갈림 | trace-05 추가 관찰 (08-27) | (미해결) 배포 전 말투 변형 반복 테스트 필요 — §5 제외 범위에 명시 |

## 9. 다음 단계

1. 주문 실행 도구 추가 시 사용자 명시적 승인 단계 설계 (M11 해커톤)
2. 가족 구성원별 계좌 인증·권한 분리 설계
3. 말투 변형에 대한 도구 호출 안정성 회귀 테스트 세트 구축
